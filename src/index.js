import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // Styles if you have them
import App from './App'; // Make sure this path is correct

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);


